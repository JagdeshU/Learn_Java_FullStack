package io.jagdesh.learn.c_string;

public class co_length {
    public static void main(String[] args) {
        String str = new String("Crows are Cameras");
        int length = str.length();
        System.out.println("\n length of the string '" + str + "' is :: " + length);
    }
}
